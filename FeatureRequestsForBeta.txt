Kanban-Builder next steps

Setup Arch thread

Setup Github

Add Name board for Save operation

Allow user to select a color to show as a "gradient under a wire mesh" in the Task's element

Tests button becomes "? Readme" which onclick loads README, README is maintained with all the current tests with an anchor link, allowing the Test button to deeplink to the test section of the README.

New Image property UX supports drag and drop file(s), in the case of 2 or more, than the property name auto numbers the user defined property name starting with 1.

Sync to shared-state backend (was Gist)


Design vision goal "Send to bot" (Fetch or take Action) where bots could be You, Claude Cowork, OpenAI, self-hosted AI.  A task could change state based on being able to do this.  Say pending review column is created if missing.

